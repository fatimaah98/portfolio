<template>
     <div class="flex flex-wrap justify-center gap-4 mb-12">
          <!-- {filters.map((filter) => (
          <button
          key={filter}
          >
          {filter === 'all' ? 'All Projects' : filter}
          </button>
          ))} -->
          filter section
     </div>
</template>